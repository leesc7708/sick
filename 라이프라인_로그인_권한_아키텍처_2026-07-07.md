# 라이프라인 — 로그인·권한·긴급알림 아키텍처 (웹 전용)

작성일: 2026-07-07
범위: 일반 아이디(이메일) 회원가입 + 3계층 역할 + 긴급버튼→관리자 실시간 알림. **웹만** (앱은 추후).
전제: Firebase(wheresick-5617a, Blaze) 재활용. Google 로그인 안 씀.

---

## 1. 역할(Role) 3계층
사용자가 말한 master/supervisor/admin을 명확히 정의:

| 코드값 | 명칭 | 대상 | 핵심 권한 |
|---|---|---|---|
| **master** | 총괄관리자 | 회사 본사(강위/대표 등) | 전체. 현장·계정 생성/삭제, supervisor 임명, 모든 현장 알림 수신 |
| **supervisor** | 현장관리자 | 현장소장·안전관리자 | **담당 현장**의 근로자 관리, **긴급알림 수신·확인**, 사고·체크 현황 |
| **worker** | 근로자(사용자) | 외국인 포함 작업자 | 긴급버튼, 표현집, 진료과 상담, 검진기록. (= 사용자가 말한 "admin") |

> 원칙: 상위가 하위를 만들고 승인. worker가 스스로 가입하면 **status=pending(승인대기)** → supervisor/master 승인 시 active.

---

## 2. 인증(Auth) — 이메일/비밀번호
- **Firebase Authentication · Email/Password** (Google·소셜 안 씀). 이메일이 곧 아이디.
- (순수 username 방식도 가능하나 `username@lifeline.local` 가짜이메일 매핑이 필요해 비번재설정·중복확인이 복잡 → **MVP는 이메일 권장**.)
- 가입 입력: 이메일·비번·이름·소속현장·역할요청(기본 worker) → users 문서 생성(status=pending).
- 콘솔에서 **이메일/비번 provider 활성화** 필요(내가 처리 or 형님 1클릭).

---

## 3. 데이터 모델 (Firestore)
```
users/{uid}       { name, email, role, siteId, status(pending|active), lang, createdAt }
sites/{siteId}    { name, region, masterUid, createdAt }
alerts/{alertId}  { workerUid, workerName, siteId, type(emergency|injury|checkfail),
                    status(open|ack|closed), location{lat,lng}, message, createdAt, ackBy, ackAt }
```
- 역할·소속은 **Auth custom claims**(role, siteId)에도 심어 보안규칙에서 사용 (Cloud Function으로 세팅).
- 기존 로컬(AsyncStorage) 데이터는 로그인 후 users 하위/Firestore로 승격(선택).

---

## 4. ★ 긴급버튼 → 관리자 실시간 알림 (핵심 플로우)
```
[worker]                    [Firestore]                 [supervisor 웹 대시보드]
 🆘 긴급버튼 누름  ──create──▶  alerts/{id}                  onSnapshot 실시간 구독
   (siteId·위치·type)          {status:open, ...}    ──push──▶ 🔴 배너 + 경고음 + 진동
                                                              "○○ 근로자 긴급! (현장/시각/위치)"
                              status:ack  ◀──확인(ack)──────  [확인] 클릭
 "관리자 확인함" 표시 ◀──────────  ackBy/ackAt
```
- supervisor 대시보드는 `alerts where siteId == 내현장 and status == open`을 **실시간 구독** → 새 알림 즉시 소리·화면.
- master는 전 현장 alert 수신. worker는 자기 alert만 생성/조회.
- **2단계 확장**: 탭이 꺼져 있어도 받도록 **FCM 웹 푸시**(서비스워커). MVP는 대시보드 열려있을 때 실시간.

---

## 5. 보안 규칙 (요지)
- users: 본인 문서 read. 본인은 제한 필드만 write(role·status·siteId는 못 바꿈). **역할 변경은 Cloud Function만**.
- alerts: worker=자기 것 create. supervisor/master=자기 siteId(master는 전체) read + status update. **삭제 금지**(기록 보존).
- 판정은 custom claims(role, siteId) 기반 → 클라이언트가 타인 문서 못 읽음.

---

## 6. 화면 (웹 전용)
- **공통**: 로그인 · 회원가입 · 승인대기 안내
- **worker**: 기존 홈(표현집·상담·검진 등) + **🆘 긴급버튼**(alert 생성, 위치 포함) + "관리자 확인함" 상태
- **supervisor 대시보드**: 실시간 긴급알림(경고음)·확인처리 / 소속 근로자 목록 / 오늘 작업전체크·사고보고 현황
- **master 어드민**: 현장 CRUD, 계정 승인·역할부여, supervisor 임명

---

## 7. 기술 구성 (추가되는 것)
- 라이프라인 웹앱(Expo web)에 **Firebase JS SDK(Auth+Firestore)** 도입 — 현재는 로컬저장+함수 1개뿐이라 신규.
- **Cloud Functions**: 역할부여(setRole → custom claim), 가입 후처리. (이미 Blaze)
- Firestore 보안규칙 + 인덱스(alerts siteId+status+createdAt).
- 로그인 상태에 따라 라우팅(worker/supervisor/master 홈 분기).

---

## 8. 단계별 구현 로드맵
| 단계 | 내용 |
|---|---|
| **P1 인증·역할** | Email/Password 로그인·가입·승인대기, users 문서, 역할 저장, 역할별 라우팅 |
| **P2 긴급알림** | worker 🆘 → alert 생성, supervisor 대시보드 실시간 수신(경고음·확인) |
| **P3 관리** | master 어드민(현장·계정·역할), 보안규칙, custom claims CF |
| **P4 확장** | FCM 웹푸시(탭 꺼져도), 위치 지도, 사고보고·검진 연동 |

---

## 9. 형님 확정 결정 (2026-07-07)
1. **역할**: master / supervisor / worker 유지. (일반사용자=미승인 상태)
2. **아이디 방식**: **아이디(username)+간편 비번**. 이메일 아님. Firebase Auth는 `아이디@lifeline.app` 가짜이메일로 내부 매핑.
   - 가입 필수입력: **아이디 · 비밀번호(간편) · 이름 · 전화번호 · 현장선택**
   - **현장 = 이천 하이닉스 / 청주 하이닉스 2개 고정** 드롭다운
3. **가입 흐름**: 자유가입 → **미승인 상태=일반사용자**(기본기능만, **AI 진료과상담 사용 불가**) → master/supervisor 승인 시 worker(AI 사용 가능)
4. **미국 정보이전 동의 게이트**: AI 기능은 "증상이 미국 AI로 전송됨" **동의해야만** 작동(이미 구현된 동의 재사용). 미동의/미승인 = AI 차단.
5. **최초 로그인 시 나라(언어) 선택** 화면. (WiiInfo 로그인 방식 참조: Firebase Auth + Firestore user 문서 + onAuthStateChanged 구조 참조. 단 구글 아니라 아이디/비번)

## 10. ★ 순수 웹 유지 (형님 지시 2026-07-07)
- **PWA/설치 유발 금지** — manifest·서비스워커 넣지 말 것. 폰에서 "앱 설치/홈추가" 팝업 뜨는 것 싫어하심(DY ON 같은 강제앱 X). 라이프라인은 **브라우저로 여는 순수 웹사이트**로 유지. 접속 = https://wheresick-5617a.firebaseapp.com

## 11. Firebase 준비 상태
- 웹 앱 등록됨: "Lifeline Web" (appId 1:782955015449:web:e25ca26b497ec236b12988), config 확보(apiKey 등)
- ⏳ 이메일/비번(아이디) 인증 provider 활성화 필요 — Identity Toolkit API 404(아직 Auth 미초기화). 콘솔 Authentication "시작하기" 1회 or API 재시도로 초기화 후 활성화
- 다음: firebase JS SDK 도입 → 로그인/가입 화면 → 역할·승인 → 긴급알림
