# 라이프라인 P0 5건 — 실행계획·아키텍처 (기획+개발 합동)

**작성**: 2026-07-08 / 기획팀·개발팀 합동 회의 결과
**대상**: 3팀 검토(라이프라인_3팀검토_회원관리_안전_동의_2026-07-08.md)에서 도출된 P0 5건
**스코프**: 반나절~1일. 순수 웹·CF 최소 원칙 유지. 코드 삭제 대신 주석처리 규칙 준수.

---

## ✅ 확정 작업 순서

```
#5 위치정보 방침 → #1 온보딩 라우팅 → #4 WorkCheck → #3 확인 다이얼로그 → #2 rules 축소
                                                                              ↓
                                              통합 2계정 e2e 게이트(14/0 재확인)
                                                                              ↓
                                              [트랙 D] 2계정 라이브 긴급 시연 녹화
```

**두 팀 합의점**: #5 최선두(무회귀 + #1의 선행 보강재) · #2 최후미(회귀 blast radius 최대, 바로 뒤 e2e가 파손 포착) · 라이브 시연은 5건 완료된 **최종 빌드 위에서 마지막**(먼저 녹화하면 이후 변경으로 무효화).

**#1 위치 근거(기획팀 채택)**: #1은 두 번째로 회귀 위험이 큼(신규·기존·재로그인 3케이스 라우팅). 오전에 넣어야 오후 e2e로 잡을 **테스트 버퍼**가 생김. 개발팀도 "#1은 #2 e2e와 같은 로그인 플로우라 같은 사이클에 검증하면 효율적"이라 정합 → #1·#2를 로그인 플로우로 묶어 최종 e2e에서 함께 검증.

---

## 1️⃣ #5 위치정보 처리방침 명시 (≈30분, 회귀 0)

**gap**: `sendEmergency`가 GPS 좌표 수집(crew.ts:133-136) → `alerts` lat/lng로 Firebase 서버 전송(138-142) → CrewScreen에서 관리자가 `map.kakao.com/?q=lat,lng` 오픈(제3자 제공)인데 `privacyPolicy.ts`에 위치정보 언급 전무.

**구현**: `privacyPolicy.ts` ko/en Policy에 "위치정보 수집·이용" 섹션 신설(기존 §4 국외이전 앞뒤, 번호 재정렬):
- 긴급 알림 전송 시에**만** 현재 위치(GPS) 수집(상시수집 아님)
- 소속 관리자 alert로 Firebase(서울) 저장·전송, 관리자 지도 확인 시 카카오맵(제3자)로 좌표 전달
- 위치권한 거부 시 좌표 없이 알림만 전송됨(코드상 try/catch로 null 허용) 명시
- `updated` 날짜 갱신. ko=법적 원문, 5개 언어 en 폴백(파일 정책 유지)

**선행 이유**: #1 온보딩이 처리방침 화면을 판정단에게 직접 노출 → 그때 GPS 항목이 이미 채워져 있어야 informed-consent 서사 완성.

---

## 2️⃣ #1 온보딩·동의 도달 경로 복구 (반나절, 회귀 위험 최고)

**문제**: 초기 라우트 Home 고정(RootNavigation:89). Onboarding은 스택 등록만·자동진입 없음(유일 진입=설정 재설정). `onboardingDone`은 `eodi:profile` 단일객체에 저장 = **기기공유·전계정 공유** → 라우팅 플래그로 쓰면 계정 A 완료 시 B가 스킵되는 버그.

**아키텍처 — onboardingDone을 uid별 로컬 키로 분리**:
- `storage.ts`: 신규 키 `lifeline:onboarded:${uid}`, `getOnboarded(uid)`/`setOnboarded(uid,v)`. 기존 `profile.onboardingDone`은 하위호환 유지(주석, 삭제 금지)
- `AuthContext.tsx`: `loadAccount(u)`에서 `getOnboarded(u.uid)` 동시 로드 → 컨텍스트에 `onboarded: boolean` 노출. loading은 account+onboarded 둘 다 끝날 때까지 유지(기존 구조 자연 확장)
- `RootNavigation.tsx`: 인증 스택 `initialRouteName={onboarded ? 'Home' : 'Onboarding'}`. loading 가드가 이미 있어 마운트 시점에 flag 확정 → 결정적
- `OnboardingScreen.tsx`: `start()`에서 `setOnboarded(user.uid, true)` 후 `replace('Home')` (useAuth로 uid 확보)

**알려진 엣지(문서화·감수)**: 딥링크 `/home` 직접진입 시 initialRouteName override → 미온보딩 유저가 Home 볼 수 있음. e2e는 루트(`/`)로 진입하므로 무영향. 추후 Home 마운트 가드 여지.

**범위 결정(기획 확정)**:
- **온보딩 강제 통과(Skip 버튼 없음)**. 하드 필수는 모드 선택 1개뿐(`disabled={!mode}` 유지). 민감정보 미입력 유저는 모드만 고르고 즉시 Start = 동의 마찰 0(법적으로 민감정보 없으면 동의 불요). 프로필은 전부 선택.
- **기존 유저 소급 온보딩**: `onboarded` 플래그 없는 전원 1회 온보딩 통과. 데모라 유저 극소(테스트+mmersum). 기존 민감데이터 보유자는 이때 동의 재확인(증적 소급 불가라 보수적으로 재확인이 맞음).
- ⚠️ **시연 계정도 기존 유저** → 녹화 시 첫 로그인에 온보딩 만남. 결함 아니라 데모 장면(언어선택·동의)으로 활용, e2e 스크립트에 이 1스텝 포함.

---

## 3️⃣ #4 WorkCheck 기본값 미선택 + tookMeds 분리 (1~1.5시간, 국소)

**문제**: `useState(true)`×4(WorkCheckScreen:26-29)=통과 프리셋(acquiescence bias). `allGood=sleepOk&&noAlcohol&&noDizziness`(30)라 tookMeds는 저장만·판정 무반영("게이트 같으나 아닌" 불일치).

**아키텍처**:
- 3개 안전문항(sleepOk/noAlcohol/noDizziness) → `useState<boolean|null>(null)`. `Question`을 tri-state화(예/아니오 둘 다 미선택 가능, Chip `selected={value===true}`/`{value===false}`)
- 제출 버튼 `disabled` = 3문항 중 미응답 존재(능동응답 강제)
- `allGood = sleepOk===true && noAlcohol===true && noDizziness===true`, result는 전원 응답 후 계산
- `types/index.ts:78` `tookMeds: boolean` → `boolean | null`. 소비처(ManagerDashboard 미사용, History 확인) 타입 회귀 점검

**범위 결정(기획 확정) — tookMeds는 판정 반영 안 함, '참고 정보'로 시각 분리**:
- 근거: "약 복용 예/아니오"는 극성 모호. "아니오"=혈압약 깜빡(위험)/원래 약 없음(정상 다수)/진정제 후 졸림(이 경우 '예'가 위험)이 뒤섞임 → "아니오=부적합"은 **약 안 먹는 다수에 대량 오탐** → 신뢰 붕괴. 진정·근이완제는 '예'가 위험이라 극성 자체가 뒤집힘
- 결정: 적합성 게이트는 수면·음주·어지럼 3개만. tookMeds는 pass/fail 카드에서 빼 별도 '참고 정보' 블록으로(로그 저장은 유지 — 사고 시 의료 맥락용). "데드 입력"→"의도적 참고 항목"으로 정직 해소
- (여유 시) 문항을 "졸음·어지럼 유발 약을 드셨나요?"로 재작성 검토하되 어지럼 문항과 중복 → 반나절 스코프에선 '참고 분리'까지만

---

## 4️⃣ #3 최고권한 변경 확인 다이얼로그 (≈1시간, UserAdminScreen 단일)

**현재**: 역할 pill `onPress={apply(u,r,'active','변경')}`(:97) 무확인 1탭. reject만 확인(:66-70). 본인 ssvisor 자가해제 가드는 존재(:53-55).

**아키텍처 — `apply` 앞에 `confirmApply(u,r)` 래퍼**:
- **ssvisor 부여**(`r==='ssvisor'`): 2단계. 1차 "총괄 권한 부여 — 전체 DB 열람" 경고 → 2차 "최종 확인: {name}에게 총괄 권한 부여"(destructive)
- **활성유저 강등/변경**(`u.status==='active' && u.role!==r`): 1단계 destructive 확인. 특히 총괄 강등(`u.role==='ssvisor' && r!=='ssvisor'`) 문구 강조
- 신규 pending 승인(첫 활성화)은 무확인 유지(마찰 최소) — **강등·최고권한만 게이트**
- 파일: UserAdminScreen 단일, Card pill onPress를 confirmApply로 교체
- **라이브 시연 전 필수**: 카메라 앞에서 역할 pill 다루므로 오조작 가드가 먼저 들어가 있어야 함

---

## 5️⃣ #2 관리자 읽기 범위 축소 (rules 수정+재배포, 회귀 최대 → 최후미 + e2e 게이트)

### 원리: "Firestore rules는 필터가 아니다"
list/query는 쿼리 shape로 안전성 증명해야 통과. rule이 `resource.data.X`를 참조하면 쿼리에 그 필드 where가 없으면 **쿼리 전체 거부**. 현재 통과 이유는 `isManager()`가 resource 무참조이기 때문 → 축소하는 순간 이 원리에 걸림.

### users·crewMemberships read 경로 전수 (축소 시 영향)
| 경로 | 위치 | 연산 | 새 rules 하 |
|------|------|------|-------------|
| fetchAccount(uid) | auth.ts:78 | get self | **OK** |
| listUsers() | auth.ts:91 | list 전체 | ssvisor만 **OK**(svisor 차단=의도) |
| **findWorkerByUsername** | crew.ts:63 | **list** users where username | **깨짐** ⚠️ |
| getMyMembership | crew.ts:117 | get self membership | **OK** |
| **watchMembers(crewId)** | crew.ts:95 | **list** memberships where crewId | **깨짐** ⚠️ |
| watchMyCrews/Alerts | crew.ts:86,104 | crews/alerts(users 아님) | 무영향 |

### 해결 A — crewMemberships(watchMembers): rules로 표현 가능 + 쿼리 shape 변경
멤버십 문서에 `ownerUid` 이미 저장(addWorker:76) → "자기 크루 소속"을 필드로 표현:
```
match /crewMemberships/{wuid} {
  allow read: if isSignedIn() && (
      request.auth.uid == wuid
      || resource.data.ownerUid == request.auth.uid   // 자기 크루 멤버
      || isSsvisor());
  allow write: (기존 유지)
}
```
- **쿼리 변경 필수**: `watchMembers(crewId)` → `watchMembers(crewId, ownerUid)`, 쿼리를 `where('ownerUid','==',ownerUid)`로, crewId·status는 **클라 필터**(`m.crewId===crewId && m.status==='active'`)
- ownerUid 단일 필드 → **자동 인덱스, indexes.json 불필요**(firebase.json 무설정과 정합)
- CrewScreen:57 `watchMembers(sel.id, setMembers)` → `watchMembers(sel.id, sel.ownerUid, setMembers)`

### 해결 B — users(findWorkerByUsername): rules만으론 불가 → **옵션 B 채택(CF 최소)**
닭-달걀: svisor가 워커를 크루에 넣기 전엔 멤버십 없음 → "내 크루 소속"으로 그 유저 read 승인 불가. username은 필드라 반드시 list. list를 svisor에 열면 곧 dump 허용.

> **옵션 B — username 조회 전용 컬렉션 (rules-only, 새 CF 0) [채택]**
> - 신규 `usernames/{username}` = `{uid, name, phone}`. 문서키=username이라 **get 조회**
> - `signup()`(auth.ts:47)에서 users 생성과 함께 `setDoc(doc(db,'usernames',username),{uid,name,phone})`
> - `findWorkerByUsername` → `getDoc(doc(db,'usernames',clean))`(list→get)
> - rules:
> ```
> match /usernames/{uname} {
>   allow get:    if isManager();
>   allow list:   if isSsvisor();
>   allow create: if isSignedIn() && request.auth.uid == request.resource.data.uid;
>   allow update: if isSignedIn() && request.auth.uid == resource.data.uid;
> }
> ```
> - users 본체: `allow read: if self || isSsvisor()`로 강축소(dump·PII 전체 차단)
> - 트레이드오프: name/phone이 매니저-read 위치에 중복(단 매니저는 크루 추가 시 어차피 봄, 노출면 축소). **기존 가입자 backfill 1회 필요**(ssvisor 도구 또는 소규모 수동 이관)
>
> **옵션 A(대안, 보안 최상)**: onCall CF `lookupWorker`(firebase-admin + 호출자 role 검증). PII 중복·backfill 불요이나 admin SDK 최초 도입 → "CF 최소" 원칙엔 B가 부합. 둘 다 users 축소 rule은 `self || isSsvisor()` 동일.

### #2 최종 rules(옵션 B)
```
match /users/{uid} {
  allow read: if isSignedIn() && (request.auth.uid == uid || isSsvisor());  // isManager→isSsvisor
  allow create/update/delete: (기존 유지)
}
match /crewMemberships/{wuid} { ...위 해결 A... }
match /usernames/{uname} { ...위 신규... }
// crews·alerts는 ownerUid/notifyUid 기반이라 무변경
```

---

## 🧪 검증·게이트 계획

**공통**: 각 항목 빌드 성공 후 커밋. #2는 rules 배포가 되돌리기 어려움 → **단독 커밋 + 배포 전 에뮬레이터 rules 통과 필수**.

**#2 (필수)**:
- (권장) `@firebase/rules-unit-testing` 에뮬레이터 단위테스트 최초 도입: svisor listUsers 거부 / watchMembers 자기크루 허용·타크루 거부 / usernames get 허용 / worker 자기 membership 허용·타인 거부 / ssvisor listUsers 허용
- 실 2~3계정 e2e: ①svisor 크루생성→워커 아이디 추가 성공(usernames get)→멤버 실시간 표시(watchMembers 신쿼리) ②worker 오늘소속·긴급전송 ③ssvisor UserAdmin 목록 로드 ④svisor UserAdmin 차단
- backfill 확인(기존 워커 usernames 문서 없으면 추가 실패 → 이관 선행)

**#1**: 2계정 순차 로그인 — A 온보딩 완료 후 **B 온보딩 재노출**(uid별 키), 재로그인 시 A는 Home 직행. 딥링크 override 엣지 문서화

**#3**: 승격/강등 각 경로 확인창 출현, 본인 총괄해제 차단 회귀 없음

**#4**: 신규 진입 3문항 미선택·제출 disabled, 부분응답 제출 불가, tookMeds 미선택 제출 가능·result 무영향, nullable 소비처 타입 회귀

**#5**: PrivacyPolicyScreen ko/en 신규 섹션 렌더·번호 정합

**게이트**: #2 직후 **통합 2계정 실백엔드 e2e 재실행(14/0)** = 5건이 크루·긴급 경로 무손상 증거

**트랙 D**: 전부 통과한 최종 빌드에서 2계정 라이브 긴급 시연 녹화(worker/svisor 첫 로그인 온보딩 1스텝 포함). 심사 4기준 전부에 꽂히는 유일 머니샷.

---

## 의존성 그래프
```
#5 (콘텐츠) ─────── 독립, #1의 선행 보강재
#3 (UserAdmin) ──── 독립
#4 (WorkCheck) ──── 독립 (types/index.ts tookMeds nullable 공유·경미)
#1 (라우팅) ─────── 독립 (AuthContext/RootNav/storage/Onboarding)
#2 (rules) ──────── 독립, crew.ts watchMembers 시그니처 변경 + usernames 신설
                    → 배포 전 에뮬레이터 + 배포 후 2계정 e2e 필수
```
파일 겹침 거의 없음 → 논리상 병렬 가능하나, **회귀 관리 위해 위 순서대로 순차 커밋** 권장.

## 심사 기여도 (기획)
- #1: 실현가능성↑↑(말/실물 일치)·활용도↑(프로필 수집→AI 안전망 실효)·사업성↑↑(Play 필수)
- #2: 사업성↑↑·확장성↑(다현장 SaaS 테넌시 데이터 격리 서사)
- #4: 창의성/활용도↑(acquiescence bias 인지·제거 = 안전 전문성)
- #3·#5: 실운영·규제 완전성(낮은 비용 빈틈 제거)
- 트랙 D: 4기준 전부 꽂히는 머니샷

## 보수적 주의(낙관편향 배제)
1. #2는 크루/멤버십 읽기를 조용히 깰 최대 회귀원 → "보안이라 급함"으로 먼저 지르지 말고 e2e 게이트 바로 앞
2. #1 소급 온보딩은 시연 계정에 첫 로그인 스텝 추가 → 데모 스크립트 갱신
3. "unfit이 로컬에만 저장→아무에게도 안 감"은 이번 스코프 밖(P1). 시연·발표에서 "관리자 즉시 보고"를 실물처럼 말하면 말/실물 갭 → 이번엔 카피를 "기록됨"까지만 정직하게, 서버 전송은 P1 명시
