# 라이프라인(Lifeline) 외부 전문가 검증 리포트

작성일: 2026-06-08
검증 방식: 5개 전문가 팀 병렬 검증 (실제 코드 `app/src` + 기획 문서 4종 + 배포본 https://lifeline-safety.web.app 정독)

---

## 0. 종합 결론

- **기획·전략·발표 스토리, @work 3종 기능(사고보고·작업전체크·검진QR·관리자) 완성도는 콘테스트 출품작 최상위권.** "산업개발 회사가 왜 일반인 병원앱?"을 본업 직결로 해소한 논리가 강력.
- **그러나 "야망과 코드의 격차"가 단일 최대 리스크.** 기획서가 자기 핵심 안전장치/차별점이라 내세운 것들의 일부가 **미구현이거나 정반대로 구현**되어 있어, 기술·의료·법무를 아는 심사위원이 데모를 클릭하면 "기획서가 거짓말"이 될 위험.
- **제출 전 P0 3건(아래)을 고치면 1등 가능성 中上 → 上.**

---

## 1. 여러 팀이 중복 지적한 최우선 발견 ★

### [P0-1] 구버전 코드 잔존 = 기획-구현 모순 + 규제 위반 (개발·의료규제·보안·심사 4팀 공통)
- `screens/SymptomResultScreen.tsx`가 **병명(긴장성 두통·위염·방광염 등) + "가능성 높음/보통/낮음" 확률 배지**를 출력 → 모든 기획서가 "금지"라 못박은 진단·확률 표시. **네비에서 주석 제외돼 있으나 파일·코드 잔존**, 재활성화 1줄로 노출.
- `services/ai.ts` `realAnalyze`가 클라이언트에서 `x-api-key` + `anthropic-dangerous-direct-browser-access`로 Anthropic 직접 호출 → 기획서가 "제거 완료"라 한 클라이언트 키 구조가 실제로는 살아 있음(dead code지만 `storage.getApiKey/setApiKey`도 잔존).
- `MedicineDetail/MedicineSearch/InteractionCheck` + `mockMedicines.ts`가 복용법 단정·"절대 함께 복용 금지" 등 **복약지도(약사 업무)** 표현 포함.

### [P0-2] 다국어가 정작 필요한 응급 화면에 미적용 (개발·UX 2팀 공통)
- `useLang`/`t()` 적용 화면은 **홈·온보딩·설정 3개뿐.** 응급신호·사고보고·증상메모·진료요약·병원찾기·서류함 등 11개 화면 + 데이터(`redFlags.ts` 증상 14종, `firstAid`, `careGuide`, `FIRST_STEPS`)가 **한국어 하드코딩**.
- "외국인 노동자(7개 언어)"가 핵심 사용자인데, **생명이 걸린 응급/응급처치 화면이 한국어 전용** → 핵심 가치 무력화.

### [P0-3] 건강검진기록 공유의 "만료·차단"이 허위 보안 표시 (UX·의료규제·보안 3팀 공통)
- `HealthRecordShareScreen`이 "🔒 30분 후 자동 만료·다운로드 차단·동의 기록"이라 표시하지만, 실제는 `https://lifeline.app/s/${recordId}` 고정 더미 링크(만료·접근제어·암호화 전무). `recordId`도 `hc_${Date.now()}`로 추측 가능.
- "만료 링크 공유"와 "관리자 직접 전송" 버튼이 **동일 함수** 호출. 미구현을 구현된 것처럼 고지 → 표시광고·개인정보 리스크.

---

## 2. 팀별 요약

### 🔧 개발 (RN/Expo)
- 강점: 의료/약사법 리스크를 구조적으로 차단한 설계, 계층 분리(storage/data/screens), 전역 맨위로 FAB 포커스 등록 패턴.
- 문제: i18n 미완(P0-2), `ImagePicker.MediaTypeOptions` deprecated(SDK56), 죽은 코드(FloatingSOS 미렌더·ai.ts), **ScrollTopFab이 footer 버튼과 겹침**, 인라인 컴포넌트 정의(Row/Info/Question), `LanguageContext` value 미메모이제이션.

### 🎨 UX / 현장 사용성
- 강점: 응급 정보 위계, 산업현장 특화 안전 카피("2차 사고 주의"), "보여주기"(QR·정보카드) UX, 큰 버튼·고대비.
- 문제: 응급 전 과정 한국어(P0-2), **119 도달 최소 4탭**(홈 직통 없음), QR 버튼 동작 불일치, AppBar 뒤로가기 작음, 전문용어(화관법·특수건진) 외국인 난해.

### ⚖️ 의료법·약사법·개인정보
- 라이브 화면은 양호(규칙기반·면책·출처표기). 치명: 구버전 진단/복약 코드 잔존(P0-1), **개인정보 동의 플로우·처리방침 부재**(온보딩 민감정보 수집), 검진공유 허위보안(P0-3), 관리자에 음주·복약 등 민감정보 공유 시 동의 UI 없음.

### 🔒 보안
- 양호: API키 입력 제거, AI 외부통신 0(현재 mock), 백엔드 청사진 프라이버시 우선 설계.
- HIGH: **민감 건강정보 AsyncStorage 평문 저장**, 검진공유 가짜 만료(P0-3), 실 AI 경로 클라이언트 키 잔존. 청사진: healthRecords 문서에 fileUrl과 메타 혼재 → 규칙만으론 "관리자=메타만" 보장 불가.

### 📊 콘테스트 심사 (경영진)
- 1등 가능성 **中上** (P0 미수정 시 中). 강한 무기: 전환 서사, 법적 강제 수요, @work 실작동, 검진 QR. 치명 약점: 증상결과 병명·확률(기획 위반), API키 "이전 완료" 거짓, **E-Gen 실시간 응급실 목업(차별점 2번 실연동 0%)**, 관리자 대시보드 로컬=가짜, 상표·도메인 미확보.

---

*상세 권고는 같은 폴더 `라이프라인_TODO.md` 참조. 검증 에이전트 5종 병렬 수행.*
