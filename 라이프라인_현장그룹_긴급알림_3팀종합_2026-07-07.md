# 라이프라인 — "그날의 현장 크루 그룹 + 긴급알림 라우팅" 3팀 종합 (기획·개발·리서치)

작성일: 2026-07-07
요약: 강위 아이디어(그날 임시 크루 그룹 + 그룹 생성 svisor에게만 긴급알림 + 강제 초대)를 3팀 검토. **셋 다 방향 타당 판정**, 단 한 가지(에스컬레이션 승계)는 반드시 보강.

---

## 0. 세 팀이 똑같이 수렴한 결론
| 핵심 | 기획 | 개발 | 리서치 |
|---|:-:|:-:|:-:|
| **당일 임시 세션**(상시 그룹 아님, workDate 종속) | ◎ | ◎ | ◎(SHEQSY activity·Raken 일별크루) |
| **라우팅 = 그룹 생성 svisor(사람 기준, notifyUid/ownerUid)** | ◎ | ◎ | ◎(PagerDuty식) |
| **강제 멤버십·알림 라우팅은 Cloud Functions로**(클라+규칙만은 보안구멍) | ○ | ◎ | — |
| **"한 명에게만"은 단일실패점 → 미응답 시 승계(에스컬레이션) 필수** | ◎ | ◎ | ◎(OK Alone·PagerDuty 표준) |
| **강제 배정은 업계 표준**(Connecteam·Busybusy·일가자) — 단 위치/건강 데이터 동의는 분리 | ○ | — | ◎ |
| **투명성**(워커가 자기 소속·관리자·수집항목을 모국어로), 상시위치추적 X | ◎ | — | ◎ |

→ **결론: 형님 아이디어대로 가되, "생성자 1명에게만"은 정상 케이스로 두고 "3분 미응답 시 확산"을 반드시 붙인다.** (안전 앱에서 알림 미도달=사고)

---

## 1. 확정 설계 (권고안)

### 그룹 = 당일 임시 세션
- `crews/{crewId}`: siteId, label(예 "A현장 3층 배관"), **workDate(그날)**, **ownerUid(생성 svisor=알림 대상)**, status(live/closed), managerUids[].
- `crews/{id}/members/{workerUid}`: 워커 스냅샷 + workState. (배열 아님 — 보안규칙 검증 위해 개별 문서)
- 워커 `users.currentCrewId`(+ownerUid) 비정규화: 긴급 시 추가 조회 없이 라우팅.
- 자정(KST) 자동 close 배치 + 수동 종료. 기록은 삭제 안 함(사고조사).

### 강제 초대
- svisor가 **이름 검색(현장 필터) → 4명 체크 → 추가**(주력). 전화번호·아이디 보조. 워커 수락 없이 즉시.
- 동명이인 대비 전화뒤4자리·국적 병기. 워커가 이미 다른 그룹이면 "이동" 확인.
- ⚠️ **타인 멤버십 쓰기는 CF만**(svisor는 `crewInvites` 요청문서만 생성 → CF가 검증 후 members+users 트랜잭션 세팅). users 컬렉션 write는 아무에게도 안 염(과거 rules 사고 교훈).

### 긴급 알림 라우팅
```
워커 🆘 → CF가 crewId→ownerUid 재확정(워커 위조 방지) → alert{notifyUid=ownerUid}
  svisor 대시보드: onSnapshot(alerts where notifyUid==나) → 🔴배너+경고음+[확인]
  3분 내 미확인 → 자동 확산(ssvisor + 같은 현장 svisor 전원)
  소속 없음(orphan) → 현장 svisor 전원 + ssvisor (절대 버리지 않음)
  ssvisor는 항상 전 현장 열람
```

### 강제초대 안전장치 (타협 불가 — 외국인/미등록 관점)
1. 투명성: 워커가 자기 소속·관리자·수집항목(긴급 시 위치)을 **모국어로** 봄. 몰래 X.
2. 상시 위치추적 안 함 — **위치는 긴급버튼 눌렀을 때만** alert에 담김.
3. 당일 만료로 데이터 수명 제한. 4. 정정/이탈 통로. 5. "긴급은 어떤 경우도 불이익 없음" 명시.
6. **긴급/그룹 기능은 "미국 AI 동의"와 분리** — 생명 기능을 동의에 묶지 않음.

### ssvisor 화면 (형님 요구: 엑셀 셀 테마)
- 행=그룹(현장·label·svisor), 열=인원/체크인율/긴급건수/미확인. **셀 색상**(적=긴급미확인/황=확인됨/녹=정상). 클릭→드릴다운. 과거 날짜 조회.

---

## 2. 업계 사례 (리서치 — 형님 아이디어 검증됨)
- **강제 배정**은 표준: Connecteam(드래그로 강제 배정), Busybusy(현장 드래그 배정), 한국 **일가자**(배정+push).
- **담당자 라우팅+미응답 승계**도 표준: **OK Alone**(Alert Loops 순환 수락), **SHEQSY**(팀별 매니저+에스컬레이션), **PagerDuty**(1차→timeout→2차).
- **임시 세션**: SHEQSY "activity"(유효시간 걸린 안전세션)가 라이프라인 "그날 크루"와 가장 근접.
- **한국 벤치마크**: **"무사고 관리자"** 앱 = 근로자 호출·위험 실시간알림·위치·발송이력 (라이프라인과 거의 동일 구상). 단 그쪽은 QR(근로자 능동), 우린 강제.
- **차별화**: 조사한 어떤 앱도 **다국어/외국인 특화 긴급 라우팅 없음**. "관리자=한국어, 근로자=모국어로 같은 긴급 이벤트" = 라이프라인만의 자리.

---

## 3. 최대 난제 (개발·기획 공통 지적) — 결정 필요
**순수 웹(PWA·서비스워커 금지) + 탭 닫힌 svisor에게 긴급 실시간 전달 = 충돌.**
- onSnapshot은 **탭 열려 있을 때만** 작동. svisor가 브라우저를 닫으면 못 받음.
- 해결책 = **FCM 웹 푸시(서비스워커 필요)**. 단 앞서 확인한 대로 **"알림 전용 서비스워커(manifest 없음)"는 앱 설치 팝업을 안 띄웁니다.** 즉 **설치 강요 없이 알림만** 가능(안드로이드). → 형님이 싫어하는 "강제 앱 설치"는 안 생기고 알림은 됨.
- **권고**: Phase 1 = 대시보드 열어두면 실시간 경고음(설치·SW 없이 전 기기). Phase 2 = 알림전용 SW로 FCM 푸시(안드, 탭 닫혀도). 이러면 "순수 웹 + 알림 됨" 둘 다 만족.

---

## 4. 단계별 구현 (P1 로그인 위에 얹음)
| 단계 | 내용 |
|---|---|
| **P2-a 그룹·멤버** | crews/members 모델 + 보안규칙 + CF(강제 멤버십). svisor "오늘 그룹 만들기→워커 추가", 워커 홈 "오늘 소속" 배너 |
| **P2-b 긴급 라우팅** | 워커 🆘 → alert(CF 재확정) → svisor 대시보드 실시간+경고음+확인. orphan 폴백 |
| **P2-c 에스컬레이션** | 3분 미확인 → ssvisor+현장 svisor 확산 (CF 스케줄/타이머) |
| **P3 ssvisor 엑셀뷰** | 전 현장 스프레드시트 색상 대시보드 |
| **P4 FCM 푸시** | 알림전용 SW(설치팝업X) → 안드로이드 탭 닫혀도 알림 |

---

## 5. ★ 형님 확정 결정 (2026-07-07)
1. **FCM 푸시 도입 = YES.** 알림 전용 서비스워커(manifest 없음 → 설치팝업 X)로 안드로이드 탭 닫혀도 긴급알림 수신.
2. **평소 알림 = 생성 svisor에게만 + 미응답(3분) 시 확산 = YES.**
3. **워커 이탈권 = 스스로 이탈 가능(self-removal).** 워커가 "나 여기 아니에요" → 즉시 그룹에서 빠짐. (자율성 우선 채택. ⚠️구현 시: 이탈하면 그 순간부터 긴급 라우팅 대상 아님을 워커에게 모국어로 명확히 고지 — 안전 감시에서 빠짐을 인지시킬 것)
4. **야간조 = 에스바이저가 그룹 생성 시 "작업 종료 예정 시각"을 직접 설정.** 자정 자동종료 안 함. svisor가 지정한 종료시각/수동종료로 close. (야간작업 그룹 안 죽음)
5. 나머지(ssvisor 엑셀 편집가능·복수소속)는 기본값 진행 후 조정.

→ **4개 결정 확정. 다음 세션에서 P2(그룹·긴급 라우팅) 구현 착수.**

*근거: 기획·개발·리서치 3팀(2026-07-07) + P1 구현 코드. 강제배정은 업계 표준으로 검증됨. 단일 라우팅의 승계 부재가 유일한 필수 보강점.*
