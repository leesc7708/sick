# 세이프콜 — 백엔드/멀티유저 청사진 (설계)

작성일: 2026-06-08
상태: **설계 단계. 구현은 승인 후 진행.**
목표: 현재 "1인 + 기기 로컬 저장" 데모 → **3단계 역할(근로자/현장관리자/회사관리자) 기반 실운영 구조**

> 이 문서는 "현장에서 진짜 쓰는" 데 필요한 구조를 정의한다. 코드는 이 청사진 승인 후 작성한다.

---

## 1. 현재 한계 (왜 재설계가 필요한가)

- 데이터가 **각자 기기 로컬(AsyncStorage)** → 관리자가 근로자 데이터를 볼 수 없음. **현재 관리자 대시보드는 사실상 가짜**(내 기기 내 데이터만).
- **로그인·계정·권한 없음**. `현장/일반 모드`는 화면 토글일 뿐 역할이 아님.
- 이미지(검진·증상 사진)를 **로컬 경로로만** 보관 → 기기 교체 시 소실, 누적 시 용량 문제.

→ 인증 + 역할 + 조직 + 서버 DB + 파일 스토리지로 재설계 필요.

---

## 2. 역할 구조 (3단계)

| 역할 | 범위 | 핵심 권한 |
|---|---|---|
| **근로자(worker)** | 본인 | 본인 검진기록·증상메모·작업체크·사고 등록/조회, QR 공유, 본인 데이터 삭제 |
| **현장관리자(siteManager)** | 자기 현장(Site) | 자기 현장 근로자의 작업체크/사고/검진**유효성** 조회, 실시간 사고 알림, 근로자 초대 |
| **회사관리자(companyAdmin)** | 회사 전체 | 현장 생성·배정, 관리자 임명, 전체 현장 통계, 정책 설정 |

권한 원칙:
- 상위 역할은 하위 범위를 포함하되, **건강 원문(검진 PDF/사진)은 근로자 동의 없으면 관리자도 못 봄.** 관리자는 기본적으로 **유효/만료 여부(메타)만** 본다.
- 사고·작업체크처럼 **안전 목적 데이터**는 소속 현장관리자에게 공유(동의를 가입 시 받음).

---

## 3. 조직 구조

```
회사(Company)
 └─ 현장(Site)  ※ 예: 온산 현장, 포항 현장
     └─ 근로자(Worker)  ※ 한 근로자는 한 시점에 한 현장 소속(이동 가능)
   └─ 현장관리자(SiteManager)  ※ 현장에 배정
회사관리자(CompanyAdmin)  ※ 회사 전체
```

- 디와이산업개발 = 1개 Company로 시작 → 추후 협력사/타사 = 별도 Company (SaaS 확장).

---

## 4. 데이터 모델 (Firestore)

```
companies/{companyId}
  { name, createdAt }

companies/{companyId}/sites/{siteId}
  { name, address, managerUids: [], createdAt }

users/{uid}
  { role: 'worker'|'siteManager'|'companyAdmin',
    name, phone, companyId, siteId(근로자/현장관리자),
    consents: { safetyShare: bool, healthShare: bool }, createdAt }

healthRecords/{recordId}
  { uid, companyId, siteId, type, fileUrl, thumbUrl,
    examDate, expireDate, result, sharedMeta: bool, createdAt }
  // 원문(fileUrl)은 본인+동의자만. 관리자 집계는 expireDate만 사용.

workChecks/{checkId}
  { uid, companyId, siteId, workType, sleepOk, noAlcohol, tookMeds, noDizziness, riskWarn, completedAt }

incidents/{incidentId}
  { uid, companyId, siteId, type, location, memo, reportedAt, status }

symptomMemos/{memoId}
  { uid, ... , private: true }   // 본인 전용. 관리자 비공개.
```

인덱스: `siteId + completedAt`, `siteId + reportedAt`, `siteId + expireDate` (관리자 집계용).

---

## 5. 파일 저장 (Firebase Storage) + 썸네일 (용량 해결)

문제: 이미지를 로컬/문서에 base64로 쌓으면 용량 폭증.
해결:
- 원본 → `storage/{companyId}/{uid}/health/{recordId}.jpg|pdf`
- 업로드 전 **expo-image-manipulator로 리사이즈**(예: 긴 변 1600px) → 원본 경량화
- 목록용 **썸네일**(예: 240px) 별도 생성 → Firestore엔 `thumbUrl`만, 목록은 썸네일만 로드
- 보기/공유 시에만 원본 URL 요청 (만료 서명 URL)

→ 데이터가 아무리 쌓여도 앱 목록은 가볍게 유지.

---

## 6. 보안 규칙 (요지)

```
// 근로자: 본인 문서만
match /healthRecords/{id} {
  allow read, write: if request.auth.uid == resource.data.uid;
  // 현장관리자: 같은 site의 '메타'만 (원문 제외는 앱/함수 레벨에서 분리)
  allow read: if isSiteManagerOf(resource.data.siteId);
}
match /incidents/{id} {
  allow create: if request.auth.uid == request.resource.data.uid;
  allow read: if isSiteManagerOf(resource.data.siteId) || ownerOf(id);
}
```
- 건강 원문은 **Cloud Functions 경유**로만 동의 검증 후 서명 URL 발급(규칙만으로 부족한 민감정보 보호).
- 회사관리자: `companyId` 일치 범위.

---

## 7. 추가 화면

| 화면 | 역할 | 내용 |
|---|---|---|
| 로그인 / 가입(초대코드) | 공통 | 전화/이메일 인증, 회사·현장 자동 배정 |
| 역할별 홈 분기 | 공통 | 근로자=현재 앱 / 관리자=관리 콘솔 |
| 현장관리자 대시보드 | siteManager | **실데이터**: 오늘 체크 현황, 실시간 사고, 검진 만료 임박 |
| 회사관리자 콘솔 | companyAdmin | 현장 생성·관리자 임명·근로자 초대·전체 통계 |
| 근로자 초대 | 관리자 | 초대코드/링크 발급 |
| 동의 관리 | 근로자 | 안전·건강 데이터 공유 동의 토글 |

관리 콘솔(현장/회사관리자)은 **웹 우선**(반응형), 근로자는 모바일 앱 중심.

---

## 8. 단계별 구현 로드맵 (Phase 2)

- **2a. 인증·역할·조직** (1~2주): Firebase Auth + users/companies/sites + 초대 가입 + 역할별 홈 분기
- **2b. 데이터 서버 이전 + Storage** (2~3주): 로컬 → Firestore 마이그레이션, 이미지 Storage+썸네일
- **2c. 관리자 실데이터 + 보안규칙** (2~3주): 현장/회사 대시보드 실집계, Firestore 규칙, 동의 기반 원문 접근(Functions)

각 단계 끝에 빌드·배포·검증.

---

## 9. 개인정보(건강정보) 보호 원칙

- 건강검진 원문은 **근로자 소유**가 기본. 관리자는 **유효/만료 메타만** 본다.
- 공유는 **동의 + 만료 링크**. 공유 이력 로그.
- 민감정보 최소 수집·암호화·동의 분리. (의료법/개인정보보호법 대비)

---

## 10. 콘테스트 전략 메모 (보수적)

- 심사 **시연**은 현재 단일 앱 + 상황극으로도 임팩트 충분.
- "**실제 사내 도입**" 설득의 핵심은 관리자가 실데이터를 보는 2c. 사업성·실현가능성 점수를 가른다.
- 디와이 기존 Firebase(디와이온) 인프라 재활용 → 바닥부터 아님.
- 리스크(낙관 배제): 백엔드는 일정·보안 부담 큼. 8/31 마감 고려해 2a→2c 순서 엄수, 무리한 동시 진행 금지.

---

*다음: 본 청사진 승인 시 Phase 2a(인증·역할·조직)부터 구현. 코드는 codex 병행 가능.*
