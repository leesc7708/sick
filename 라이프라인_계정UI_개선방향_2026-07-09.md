# 라이프라인 계정 UI/UX 개선방향 (확정) — 2026-07-09

> 3팀(모바일UX·외국인접근성·프로덕트IA) 리뷰 후 강위 결정으로 방향 확정.
> **현재는 방향 고정만. 구현은 E-Gen(중증질환+외상센터) 백그라운드 작업 완료 후 착수.**

## 결정 (강위 확정)
1. **계정 진입점 = 우상단 상시 아이콘** (하단탭 추가 아님)
2. **범위 = P0 + P1 전부 진행**
3. 지금은 방향만, 구현 보류

---

## 현재 실태 (코드 기준)
- 로그인/가입: 있음(`LoginScreen`/`SignupScreen`, 미로그인 강제 게이팅 `RootNavigation`)
- 로그아웃: **Settings 안에만**(`SettingsScreen.tsx:123`) → 3뎁스 깊이
- 계정정보: Home 작은 텍스트(`HomeScreen.tsx:108`, 탭 불가) + Settings 카드(`SettingsScreen.tsx:114`)로 **분산**
- 계정 전용 화면/진입점: **없음**. "내 기록·설정"에 기록+설정+계정 혼재

---

## 구현 스펙 (착수 시 이대로)

### P0-1. 우상단 상시 계정 아이콘 진입점
- 주요 화면(최소 Home) **우상단에 계정 아이콘 버튼**(사람/프로필 라인아이콘, `Icon.tsx` 기존 `user` 활용). 탭 → `Account` 화면.
- 미승인(pending) 시 아이콘에 **주황 점(dot) 배지**로 상태 암시.
- 우선 Home부터, 여력되면 공통 헤더 컴포넌트로 확장.

### P0-2. "내 계정"(Account) 화면 신설 + 로그아웃 이관
- 새 화면 `AccountScreen.tsx`, 라우트 `Account`(path `account`) — `RootNavigation`(user&&onboarded 블록)·`types.ts RootStackParamList`·linking config 등록.
- 담을 것: **이름·아이디·역할·승인상태·소속현장(크루)·로그아웃**.
- 로그아웃을 여기로 이관(표준 위치). ⚠️ 코드보존 규칙: Settings의 계정카드/로그아웃은 **삭제 말고 주석처리**하고 Account로 이전.
- 소속현장: worker면 `getMyMembership`으로 "오늘 소속" 표시(Home 로직 재활용).

### P0-3. 역할·승인상태 배지 (아이콘+색+다국어)
- 공통 `RoleBadge`/`StatusBadge` 컴포넌트: `worker/svisor/ssvisor` → 아이콘+색, `pending/active/rejected` → 색배지.
- **7개 언어 라벨**(translations `t()` 패턴). 역할 쉬운 설명(예: worker="근로자", svisor="현장 관리자", ssvisor="총괄 관리자")도 다국어.

### P1-4. 미승인 안내 배너
- `status==='pending'`이면 Account(및 Home 상단)에 "관리자 승인 후 긴급알림 등 사용 가능" 배너(다국어). 무엇이 제한되는지 명시.

### P1-5. "내 기록·설정" 분해
- Home의 "내 기록·설정" 단일 진입 → **계정(우상단) / 설정 / 기록** 분리. Settings는 설정 전용으로 슬림화(언어·개인정보처리방침·앱정보 등만).

---

## 착수 시 건드릴 파일 (예상)
- `src/screens/AccountScreen.tsx` (신규)
- `src/navigation/RootNavigation.tsx` (라우트+linking)
- `src/types.ts` (RootStackParamList)
- `src/screens/HomeScreen.tsx` (우상단 아이콘 + "내 기록·설정" 분해)
- `src/screens/SettingsScreen.tsx` (계정카드·로그아웃 이관, 주석보존)
- `src/components/` (RoleBadge/StatusBadge, 기존 Icon.tsx 활용)
- `src/i18n/translations.ts` (계정/역할/상태/미승인 키 7개 언어)

## 검증 기준 (착수 후)
- `tsc --noEmit` 통과 + `npm run build` 성공
- 실브라우저: 우상단 아이콘→계정화면→로그아웃 동작, 미승인 배너 노출, 역할/상태 배지 다국어 확인
- 기존 데모(로그인·홈·응급·병원찾기) 회귀 0
- DY ON 등 타 프로젝트 참조 넣지 않음

## 미결(착수 시 정할 것)
- 우상단 아이콘을 Home만 vs 전 화면 공통 헤더로
- Settings에 계정 요약을 남길지(중복) vs 완전 이관
