# 라이프라인 — 2계정 긴급알림 라이브 시연 녹화 대본

작성일: 2026-07-10 · 목적: 심사 P0 "2계정 라이브 긴급알림 시연 녹화(머니샷)" — 형님이 이 순서대로 녹화만 하면 됨
라이브: https://wheresick-5617a.web.app · 소요: 준비 5분 + 녹화 40초 내외
핵심 장면: **워커가 🆘 누름 → 관리자 화면에 "🔴 긴급 1건"이 실시간으로 뜸 → 관리자가 [확인]** (실 Firestore 동기화)

---

## 0. 준비물
- **화면 2개**: PC 브라우저 창 2개(한 화면 좌우 분할이 머니샷에 제일 좋음) 또는 PC+휴대폰.
  - 좌 = **관리자(떠블에스바이저)** 계정 로그인
  - 우 = **워커(근로자)** 계정 로그인
- 각 창은 **다른 브라우저 프로필/시크릿창**으로 로그인해야 계정이 안 섞임(예: 좌=일반 크리엘창, 우=시크릿창).
- 화면 녹화: Windows `Win+G`(게임바) 또는 OBS. 좌우 두 창이 한 프레임에 나오게.

---

## 1. 계정·크루 1회 세팅 (녹화 전, 한 번만)

### 1-1. 관리자 계정(떠블에스바이저) 확보
- 관리자 이메일은 **master 화이트리스트**에 있어야 함(firebase-config의 master 목록). 이미 tmzkt2/mmersum 등 설정돼 있으면 그 계정 사용.
- 로그인 → 홈에 **"회원 승인·역할 관리"** 버튼이 보이면 관리자 권한 정상.

### 1-2. 워커 계정 가입 + 승인
1. 우측 창(시크릿)에서 **새 계정 가입**(예: 아이디 `demo_worker`).
2. 좌측(관리자) → **회원 승인·역할 관리(UserAdminScreen)** → 방금 가입한 `demo_worker`를 **[근로자]로 승인**.
   - 승인되면 워커 status=active.

### 1-3. 크루 생성 + 워커 배치
1. 좌측(관리자) → **현장 그룹 · 긴급 관리(CrewScreen)**.
2. 작업 그룹 이름 입력(예: `온산 3층 배관`) + 종료시각(예: `오늘 22:00`) → **크루 생성**.
3. 생성된 크루 선택 → **워커 아이디**에 `demo_worker` 검색 → **추가**.
4. 우측(워커)에서 새로고침 → 소속(멤버십)이 잡혔는지 확인(홈 하단/크루에 소속 표시).

> ⚠️ 워커가 크루에 **소속돼 있어야** 🆘가 관리자에게 감(미소속이면 "no-crew"라 전송 안 됨). 1-3까지 꼭 완료.

---

## 2. 녹화 (본편 · 약 40초)

**녹화 시작.** 좌=관리자 CrewScreen("🔴 긴급 0건" 또는 긴급 카드 없음 상태), 우=워커 홈이 보이게.

1. **(2초) 상황 내레이션**(자막 or 음성): "워커가 현장에서 이상을 느끼고 긴급 신호를 보냅니다."
2. **(워커·우측) 홈의 🆘/응급신호 → 긴급 전송** 누름.
   - 위치 권한 팝업 뜨면 **허용**(GPS 좌표가 알림에 실림 — 머니샷에 "위치 첨부" 강조 가능). 거부해도 전송은 됨.
   - "전송됨" 토스트 확인.
3. **(3~5초 안, 좌측·관리자) 자동으로 "🔴 긴급 1건" 카드가 나타남** — 새로고침 없이 실시간.
   - 카드에 **워커 이름 · 크루(작업그룹) · 위치**가 뜨는 걸 클로즈업.
4. **(관리자) [확인] 버튼** 누름 → 긴급 건 ack 처리(카드 상태 변경).
5. **(2초) 마무리 자막**: "실제 Firestore 실시간 동기화 — 목업 아님. 관리자가 즉시 인지·대응."

**녹화 끝.**

---

## 3. 촬영 팁 / 리스크
- **실시간이 안 뜰 때**: 워커가 크루 미소속(1-3 누락)이거나, 두 창이 같은 계정으로 로그인된 경우. → 계정 분리·소속 재확인.
- **위치 권한**: 회사 네트워크/브라우저 정책상 거부될 수 있음. 거부돼도 알림은 가고 "위치 미첨부"로 표시 → 시연엔 문제 없음. 좌표를 꼭 보이려면 휴대폰(우측)에서 하는 게 성공률 높음.
- **머니샷 강조점**: "새로고침 안 했는데 좌측에 알림이 스스로 떴다"가 핵심. 마우스로 좌측을 건드리지 말 것(자동 갱신임을 보여줘야 함).
- 녹화본은 사업성/데모 패키지에 첨부(제출 요건: 시연 영상).

---

## 4. 검증 상태 (2026-07-10)
- 코드 경로 확인: 워커 `HomeScreen.sendEmergency()` → `alerts` 컬렉션 생성(notifyUid=크루 ownerUid) →
  관리자 `CrewScreen.watchMyAlerts()`가 실시간 수신 → `ackAlert()`. **로직·rules e2e는 과거 14/0 통과(기록).**
- ⚠️ **UI 라이브 실클릭 녹화만 미완** = 이 대본의 대상(형님 실행). 클로드가 대신 녹화 불가.
